---
import Layout from '../layouts/Layout.astro';
import AdminDashboard from '../components/AdminDashboard.jsx';

// NOTA: La lógica de protección de ruta real depende de cómo implementes
// Supabase Auth en el cliente (ej. usando cookies o middleware).
// Esto es un esquema conceptual.
---

<Layout title="Mi Panel de Control">
  <!-- Esta directiva client:only asegura que React se ejecute solo en el navegador, 
       evitando problemas con el SSR de Astro y las sesiones de usuario -->
  <AdminDashboard client:only="react" />
</Layout>
